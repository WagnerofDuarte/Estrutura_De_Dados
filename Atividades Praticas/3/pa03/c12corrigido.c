#include <stdio.h>

// x nao possui nenhum valor inicial, logo a comparacao nunca será feita. Deve-se dar um valor inicial ao x.

int main()
{
    int x = 0;
    if(x == 0) {
        printf("X is zero"); 
    }
    return 0;
}
